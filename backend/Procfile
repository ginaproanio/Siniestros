web: python -c "from app.database import engine; engine.execute('DROP TABLE IF EXISTS alembic_version CASCADE'); print('Alembic version table cleared')" && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT
