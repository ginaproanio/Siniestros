[build]
builder = "railpack"

[deploy]
startCommand = "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"

# Configurar volumen persistente para archivos subidos
[[volumes]]
source = "uploads"
destination = "/app/uploads"
initialSync = "emptyDir"
